name: Check Results Calcio

on:
  schedule:
    - cron: '0 21 * * 0'   # domenica alle 21:00 UTC
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Restore bets.json from scanner cache
        uses: actions/cache@v4
        with:
          path: bets.json
          key: market-hunter-${{ runner.os }}-${{ github.run_id }}
          restore-keys: market-hunter-${{ runner.os }}-
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install requests
      - name: Run results checker
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python results_checker.py
